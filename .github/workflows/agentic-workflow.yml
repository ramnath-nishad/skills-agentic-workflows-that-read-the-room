name: Agentic Workflow
on:
  issues:
    types: [opened, edited]
  pull_request:
    types: [opened, edited]

jobs:
  agentic-task:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/github-script@v7
        with:
          script: |
            console.log('Agentic workflow running');